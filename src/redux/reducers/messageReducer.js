const messagesInitialState = {
  name: "vasya",
  surname: "gliba",
  age: 25,
};
const messageReducer = (state = messagesInitialState, action) => {
  return state;
};

export default messageReducer;
